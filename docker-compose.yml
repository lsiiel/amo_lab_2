version: '3'

services:
  pipeline:
    image: python:3.9-slim
    container_name: ml_pipeline
    working_dir: /app
    volumes:
      - .:/app
    command: >
      bash -c "
      chmod +x pipeline.sh && 
      ./pipeline.sh
      "